const target=new Date("2028-01-25");const boot=document.getElementById("boot"),app=document.getElementById("app");setTimeout(()=>{boot.style.display="none";app.classList.remove("hidden")},2200);function tick(){const d=Math.max(0,target-new Date());const days=Math.floor(d/86400000),weeks=Math.floor(days/7);document.getElementById("count").textContent=`${weeks} weeks remaining (${days} days)`}tick();setInterval(tick,60000);if("serviceWorker" in navigator)navigator.serviceWorker.register("service-worker.js");